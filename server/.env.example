# Copy to .env and fill in values. Never commit .env.
#
# Deployment steps:
#   1. cp .env.example .env  and fill in all values below
#   2. Replace DOMAIN_PLACEHOLDER in nginx/nginx.conf with your actual domain
#   3. Run ./init-letsencrypt.sh <domain> <email> once to obtain the first certificate
#   4. docker compose -f docker-compose.prod.yml up -d
#   5. Cert auto-renews every 12h via the certbot container

# MongoDB
MONGO_USER=telegrammer
MONGO_PASSWORD=change-me

# JWT
JWT_SECRET=change-me-use-long-random-string

# SMS: "console" (logs OTP to stdout) or "twilio"
SMS_PROVIDER=console
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Your domain (replace DOMAIN_PLACEHOLDER in nginx/nginx.conf with this value)
DOMAIN=chat.example.com
